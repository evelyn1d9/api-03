"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var fs_1 = require("fs");
var path_1 = require("path");
function readDirectory(currentFolder) {
    var items = (0, fs_1.readdirSync)(currentFolder, { withFileTypes: true })
        .flatMap(function (file) { return file.isDirectory()
        ? readDirectory((0, path_1.join)(currentFolder, file.name))
        : (0, path_1.join)(currentFolder, file.name); });
    return items;
}
exports.default = readDirectory;
